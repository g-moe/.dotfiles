{
	"$schema": "https://www.raycast.com/schemas/extension.json",
	"name": "better-time",
	"version": "1.0.0",
	"private": true,
	"description": "Copy and compare Unix millisecond timestamps across time zones.",
	"categories": [
		"Applications"
	],
	"license": "MIT",
	"author": "g-moe",
	"scripts": {
		"build": "ray build -e dist -o dist",
		"dev": "ray develop",
		"test": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test tests/*.test.ts",
		"typecheck": "tsc -p tsconfig.json"
	},
	"dependencies": {
		"@raycast/api": "1.104.23",
		"zod": "4.4.3"
	},
	"devDependencies": {
		"typescript": "6.0.3"
	},
	"icon": "icon.png",
	"commands": [
		{
			"name": "now",
			"title": "Now",
			"description": "Copy the current Unix timestamp in milliseconds",
			"keywords": [
				"now"
			],
			"mode": "no-view"
		},
		{
			"name": "time",
			"title": "Time",
			"description": "View a Unix millisecond timestamp across time zones",
			"keywords": [
				"iso",
				"timestamp",
				"timezone",
				"utc"
			],
			"mode": "view"
		}
	],
	"platforms": [
		"macOS"
	],
	"title": "Better Time"
}
