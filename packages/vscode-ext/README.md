# better-vscode

VS Code extension scaffold for editor productivity tools.

Current domain module: `better-errors`, which focuses on copying the full raw editor diagnostic into an LLM-ready prompt.

## Development

```bash
npm install
npm run compile
```

## Local installs

```bash
npm run install:cursor
npm run install:codium
```
