interface:
  display_name: "GitHub PR"
  short_description: "Create or update a draft PR for the current branch"
  default_prompt: "Use $gh-pr to create or update the draft pull request for the current branch."
policy:
  allow_implicit_invocation: false
