interface:
  display_name: "Output HTML"
  short_description: "Create polished standalone HTML artifacts"
  default_prompt: "Use $output-html to create a polished standalone HTML artifact."

policy:
  allow_implicit_invocation: false
