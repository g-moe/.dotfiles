interface:
  display_name: "Effective HTML"
  short_description: "Create polished standalone HTML artifacts"
  default_prompt: "Use $effective-html to create a polished standalone HTML artifact."

policy:
  allow_implicit_invocation: false
