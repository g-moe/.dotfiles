interface:
  display_name: "Bro"
  short_description: "Restate the last message in plain language"
  default_prompt: "Use $bro to restate the message without jargon."
policy:
  allow_implicit_invocation: false
