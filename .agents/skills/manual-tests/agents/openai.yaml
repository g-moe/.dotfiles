interface:
  display_name: "Manual Tests"
  short_description: "Fix tests without hiding runtime bugs"
  default_prompt: "Use $manual-tests to update tests for these code changes."
policy:
  allow_implicit_invocation: false
